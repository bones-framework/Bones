git:
    submodules: false

before_install:
    - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
    - git submodule update --init --recursive
    - apt update
    - apt upgrade gcc make
    
jobs:
    include:
        - stage: compile
          script: make
stages:
    - compile 
